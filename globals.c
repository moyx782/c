#include "ghost.h"

// 在单一翻译单元中定义全局数据，避免头文件重复定义导致的链接错误
char start[5][5] = {
    {'#', '#', '#', '#', '#'},
    {'#', '.', '.', '.', '#'},
    {'#', '.', '#', '.', '#'},
    {'#', '.', '.', '.', '#'},
    {'#', '#', '#', '#', '#'}};

// 默认空地图（大小与 ghost.c 中边界检查一致）
char map[25][35] = {0};
